# Server Configuration
NODE_ENV=development
PORT=8080

# JWT Secret (for authentication tokens)
JWT_SECRET=your_development_jwt_secret_change_this_in_production

# MongoDB Connection String (use your actual connection string if you have one)
DB_CONNECTION_STRING=mongodb://localhost:27017/groceryapp

# You can add other configuration variables here as needed 